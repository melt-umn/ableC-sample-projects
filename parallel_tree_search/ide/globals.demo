global ide_specification {
  treesitter_parser = @joeblanchard/tree-sitter-ablec4pts;
  language_name = ablec4pts;
  file_extensions = [xc];
  first_line_regex = /\/\/ableC for PTS.*/;

  prefix "ADT" {
    atomMarkupName = comment;
  };

  prefix "RX" {
    atomMarkupName = comment;
  };
}
